apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mq-nativeha-server
  namespace: ibm-mq
spec:
  secretName: mq-nativeha-server-tls
  commonName: mq-nativeha
  dnsNames:
    - mq-nativeha
    - "*.apps.itz-c3hv3t.hub04-lb.techzone.ibm.com"
  issuerRef:
    name: servers-ca-issuer
    kind: ClusterIssuer
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - digital signature
    - key encipherment
    - server auth
